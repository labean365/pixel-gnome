# PixelGnome v0.25.0 — Release Notes

**Date:** 2026-06-20
**Type:** Stable — feature (Tier C unified file-utility integration: C1 + C2 + C5).
**Against:** v0.24.0

## Headline

PixelGnome closes the seam between its image and PDF pipelines. A dropped PDF no longer jumps into a separate, parallel modal — it enters through the **same front door** as images, appears as a **card in the same workspace**, and opens into a **drill-in editor that mirrors the image editor**. This is the first slice of the "local-first file utility for images **and** PDFs" direction: the center of gravity is the task, not the file type.

## Unified intake (C1)

- **A dropped PDF is now a card in the shared workspace**, alongside images, instead of routing to a separate tab. The card shows a first-page thumbnail, a `PDF · {n}p` badge, and filename + page-count + size — a visual sibling of the image cards, with the same select / remove / clear behaviour. One drop zone, mixed file types welcome.
- The first-page thumbnail and page count are read headlessly via the same lazy MuPDF worker the editor uses (no UI, no modal state touched); the thumbnail object URL is tracked and revoked per-card on remove and globally on Clear All.

## PDF drill-in editor (C2)

- **Click a PDF card to drill into a full editing surface** whose primary content is a **page-thumbnail grid** (rendered lazily on scroll). Pages support multi-select, **drag-to-reorder** (plus `Alt`+`←`/`→` keyboard reorder), **rotate ±90°**, **delete**, **extract**, and **remove**.
- **Edits batch and stay non-destructive until committed.** A bottom edit bar shows a preflight summary (`7 of 8 pages · 2 rotated · original unchanged`) with **Apply** (bake the edits back onto the card), **Export** (download the edited PDF without changing the card), and **Reset**.
- **Compress, Split, and To-Images are now collapsible panels inside the drill-in** rather than separate top-level tabs.

## Consistency pass (C5)

- **The PDF drill-in is now a full-screen editing surface** mirroring the image editor (`.crop-modal`) — app background, surface-colored header, content centered in a comfortable reading column — instead of a small card floating on a dark backdrop, so the two editors read as one app.
- **Aligned details:** the filename renders in the same monospace face, preset chips pick up the image editor's accent-tint hover treatment (was border-only), and the page grid grows to use the full-screen height.
- **Copy aligned:** the drill-in's primary action is now **Apply** (matching the image editor) and its dialog label is **Edit PDF** (mirroring **Edit Image**). EN and IT updated together.

## Next in this arc

- The standalone PDF modal/tablist is retired in favour of the card + drill-in flow. **C3 Recipes** (task-oriented entry spanning both file types) and **C4 mixed-selection export** are the next steps.

## Verification

- `npm run verify` green: `eslint .` 0 errors (pre-existing warnings only), Prettier clean, `build:all` succeeds, `check:pdf` passes on `dist` and `dist-single`.
- Push landed: `origin/main` = `adbc796` (release commit), `v0.25.0` tag moved onto it; GitHub `main` `package.json` = 0.25.0; IONOS Deploy #82 green.

## Files

- `package.json` — version → 0.25.0
- `src/style.css` — full-screen drill-in surface; mono filename; preset hover; taller page grid
- `src/locales/en.js`, `src/locales/it.js` — `Apply`, `Edit PDF` copy
- `src/modules/pdf/pdf-ui.js` — (formatting) + drill-in markup from C1/C2
- `CHANGELOG.md` — v0.25.0 entry
- `docs/releases/v0.25.0-{release-notes.md,tag-message.txt}`
