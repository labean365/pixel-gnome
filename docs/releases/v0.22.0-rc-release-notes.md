# PixelGnome v0.22.0-rc — Release Notes

**Date:** 2026-06-16
**Type:** Release candidate — feature (workflow) + i18n copy refresh
**Against:** v0.21.0-rc

## Headline

A first-class **convert & compress** path: change an image's format and/or shrink its file size **without changing its dimensions**. Previously every preset resized, and keeping dimensions meant faking it with an oversized box plus the *Never upscale* guard — there was no direct route for the most common reason people reach for the tool.

## What's new

**"Original size — convert & compress" preset.** Surfaced in its own **Original** group pinned to the top of the preset dropdown, with a matching **Convert & compress** chip on the empty-state landing. Defaults to WebP output at quality 0.82 (format is freely changeable); filename pattern is `{name}-min` so a same-format recompress doesn't collide with the source.

**Real no-resize mode in the engine.** `calculateDimensions()` (`src/modules/shared/dimensions.js`) gains an explicit `original` mode that returns the source dimensions untouched, rather than leaning on oversized targets + the upscale clamp. A matching **"Keep original size (no resize)"** radio is now the first option in the **Mode** group, so the choice is reachable manually as well as via the preset.

**Format & Quality surfaced for this path.** Picking the Original preset auto-expands the **Customize** panel and hides the now-irrelevant Width/Height inputs, so **Format**, **Quality**, and **Target file size** are the visible controls instead of being folded away. Single source of truth: the existing controls are reused, not duplicated.

**First-run default hardening.** `getDefaultPreset()` returned `BUILT_IN_PRESETS[0]`; adding the new preset to the top of the array would have silently changed the first-run default. It's now pinned explicitly to `full-hd`, so the array order is free to change without side effects.

## Italian copy refinements (still draft, pending native review)

A naturalness pass over `src/locales/it.js` from a translation QA punch list. Most flagged "feels untranslated" items were already fixed in v0.21.0-rc; the remaining changes are wording improvements: tagline → "Ridimensiona le immagini direttamente nel browser"; drop-zone hint → "clicca per selezionarle"; privacy note "memorizzato" → "salvato"; "Incolla codice SVG" → "Incolla SVG"; "Dimensioni esatte (ritaglio)" → "…con ritaglio"; "Non ingrandire mai" → "Non ingrandire"; target size → "Peso file desiderato"; "Esportazione multi-dimensione" → "Esporta in più dimensioni"; export helper → "Disponibile dopo il passaggio 1"; Clear-all → "Rimuovi tutto"; preview stats Size → "Peso" and Saved → "Risparmio"; footer → "Creato da 321Enterprise. Hai bisogno di uno strumento web su misura? Contattaci."

**Intentionally deferred:** the Output section heading was left as "Output" (translating it to "Esportazione" would collide with the Export step), and Italian comma-decimal number formatting (e.g. "1,39 MB") is a separate locale-aware formatting change in JS, not a dictionary swap.

## Scope / non-goals

- **No default-behavior change.** Resizing presets are untouched; the first-run default stays Full HD. The new path is opt-in.
- **Italian remains a draft** pending native review.

## Verification

- `eslint .` → 0 errors (pre-existing warnings only).
- Multi-file and single-file production builds both pass; footer reads **v0.22.0-rc**; the convert/compress preset is present in both.
- `original` mode returns source dimensions; lossless pass-through unaffected.
- `it.js` parses; EN/IT key parity maintained.

## Files

- Changed: `src/modules/shared/dimensions.js` (`original` mode), `src/modules/presets.js` (new preset + `getDefaultPreset` pin), `src/modules/settings.js` (dropdown group order, auto-open, hide dimensions), `src/index.html` (no-resize radio, empty-state chip, footer version), `src/locales/en.js`, `src/locales/it.js` (new keys + naturalness pass), `src/modules/changelog-modal.js`, `package.json`, `CHANGELOG.md`.
- Docs: this release set (`v0.22.0-rc-release-notes.md`, `v0.22.0-rc-tag-message.txt`).

## Known follow-ups

- `npm run build:single`'s post-build sanity check still looks for `id="versionBadge"` (renamed to `footerVersion` in an earlier release), so it exits non-zero on the version check. This build was produced with the same Vite config + stray-`.js` cleanup and verified manually. Fixing the check to read `footerVersion` is a small standalone task.
- Native-speaker review of the Italian draft.
- Italian comma-decimal number formatting (preview card stats).
